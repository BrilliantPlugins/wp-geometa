WP-GeoMeta Control Panel

$plugindir = explode(PATH_SEPARATOR, WP_PLUGIN_DIR); // full path, no trailing slash
$ourdir = explode(PATH_SEPARATOR, plugin_dir_path( __FILE__ ));
if ( 1 === count( array_diff($plugindir, $ourdir) ) ) {
	//show control panel
}
*/

* List of available MySQL spatial functions
* List of WP-GeoMeta installs -- with versions and parent plugins?
* MySQL version
* Spatial table list
* Spatial indexes enabled?
* Number of spatial records stored
* Map of spatial data with paging and basic filtering?
* Link to supported function matrix
* Link to WherePress.com
* List of post types with spatial data?
* Uninstall button
* Truncate / rebuild database (with progress bar?)
